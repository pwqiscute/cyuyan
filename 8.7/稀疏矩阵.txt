0的数量大于百分之95叫做稀疏元素；

用（i，j，aij）这样的三元组来存储，i是行，j是列，aij是数值；


优点：按行依次有序存储，便于矩阵运算

缺点：不能随机存取，若要存取某一行的非0元素，则需从头开始查找；

可以使用十字链表解决缺点
十字链表有一个数据域存储（i，j，aij），还有一个指向与其同一行的下个元素的行指针；还有一个指向同一列的下一个元素的指针；

每一行和每一列都有一个头指针；分别指向每一行，每一列的第一个元素；