程序的执行过程
1.程序必须载入内存中，在有操作系统的环境中，一般由操作系统完成，在独立的环境，程序的载入必须由手工安排，也可能通过可执行代码置入只读内存来完成；
2.程序的执行便开始，接着调用main函数；
3.开始执行程序代码，这个时候程序将使用运行时堆栈（函数栈帧stck），存储函数的局部变量和返回地址，程序同时也使用静态内存，存储于静态内存的变量在程序的整个执行过程一直保留它们的值；
4.终止程序，正常终止main函数，也可能意外终止；