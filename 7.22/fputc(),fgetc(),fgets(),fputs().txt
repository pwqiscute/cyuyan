在“r“的状态，r-》read（读），从文件流中读东西 fgets（放的地方，放的个数，读的地方 ）；读取成功放回放的地点；
fgets(arr, 5, pf);//----------把pf的数据读到的字符串拷贝到啊arr数组中，最多5个（其实4个，还有一个'\0')；
fgetc(读的地方）；//读一个字符，放到int 类型里；
  
在“w”的状态，w-》（写），把东西写到流里  fputc（要写的东西，放的位置）；写一个字符；返回值int；
fputs（写的字符串，文件）//写一行字符；int 返回值